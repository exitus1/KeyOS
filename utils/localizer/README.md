# Localizer

Localizer is a small rust utility for converting localization information from
Localazy format to .po/.mo format for use in Slint.

It splits out translations for multiple apps to separate files for each app, but also
includes common translations in each app's .po file.
